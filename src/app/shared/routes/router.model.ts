export enum RouterClient {
    LOGIN = 'login',
    REGISTER = 'register',
    SCORING = 'scoring',
    SUMMARY = 'summary',
    SETUP = 'setup',
}

export enum ProfileLinkTypes {
    EDIT = '/edit',
    TIMELINE = '/timeline'
}
