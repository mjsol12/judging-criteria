export enum RouterClient {
    LOGIN = 'login',
    REGISTER = 'register',
    SCORING = 'scoring',
    SETUP = 'setup',
}

export enum ProfileLinkTypes {
    EDIT = '/edit',
    TIMELINE = '/timeline'
}
